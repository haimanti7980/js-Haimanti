"use strict";
/* treat all js code as newer version but now by default it takes 
as newer version so not mandatory to use this 
*/

// you can use ; but it's not throwing an error if you don't use ;

//alert(3 + 3); // throwing error because we are using nodejs not browser

//console.log("Diu")  console.log(6);  // wrong ( for that need to use ; but not readable)

console.log(3 + 3);

console.log( 3 
    + 3)   //code readibility should be high


    let name = "Hoi" //string
    let age = 24  //number
    let isLoggedIn = true //boolean
    let state = null;

//number => range - 2 to the power 53
//bigint 
//string => use always "" ('' also right)
//boolean => true/false
//null => standalone value 
//undefined => 
//symbol => unique


//object

console.log(typeof "Hoi") //string
console.log(typeof age)

console.log(typeof null)  //object
console.log(typeof undefined) //undefined